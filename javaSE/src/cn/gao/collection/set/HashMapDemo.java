package cn.gao.collection.set;

import java.util.HashMap;

public class HashMapDemo {
   /*
   * 1.7
   *    数组加链表
   *
   * 1.8
   *    数组加链表+红黑树
   *  HashMap  hashMap =new HashMap();
   *  hashMap.put(key,value);
   *  插入之前
   *    判断hashMap内的数数组对象是否为空
   *        为空--HashMap()不传值，内置的数组初始化（），初始化的值来自内置的默认数组阈值大小是16，负载因子是0.75
   *            初始化完了数组不为空
   *        不为空--计算存储位置(n - 1) & hash    n表示数组长度
   *
   *
   *  实际步骤，通过构造函数初始化
   *
   *
   * */
}
